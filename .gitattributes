# >>> skeeper lockfile merge driver >>>
skeeper.lock merge=skeeper-lock
# <<< skeeper lockfile merge driver <<<

# manter LF: skeeper grava o lock em LF e autocrlf=true re-suja a entrada
skeeper.lock -text
